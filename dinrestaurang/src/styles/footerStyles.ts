import styled from "@emotion/styled";
import { layout, space, color, grid, typography } from "styled-system";

export const FooterBlock = styled("footer")(
  layout,
  space,
  layout,
  color,
  grid,
  typography
);
